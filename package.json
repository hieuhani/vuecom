{
  "name": "vuecommerce",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run --parallel dev"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.3.5",
    "@commitlint/config-conventional": "^8.3.4",
    "husky": "^4.2.1",
    "lerna": "^3.20.2"
  },
  "dependencies": {
    "typescript": "^3.7.5"
  }
}
